/*
 * This is just a simple JS file to easily play around with $horthand.
 */

$H(()=>{
    
    const $td = $H('.test-div');
    
    const $p = $H('<p/>').text('hello world').appendTo($H('body'));
    
});
